# Python dependencies. Install with:  pip install -r requirements.txt
#
# certifi — HTTPS certificate verification for Retrosheet downloads.
# pybaseball — Baseball Savant / Statcast pitch-level downloads (modern era).
# pytest — test runner.

certifi==2026.5.20
pybaseball==2.2.7
pytest==9.0.3
